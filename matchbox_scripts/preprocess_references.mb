names = csv('names_sorted.csv')

if names.contains({name= read.id}) => {
	cysteine_location = read.seq.to_str().to_upper().translate().find_last('C') * 3
	before_cysteine = read.seq.slice(0, cysteine_location)

	'>{read.id}\n{before_cysteine}'.stdout!()
}